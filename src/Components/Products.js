import React from "react";
import Proptypes from 'prop-type';

const Products = (props) => {
    const { product } = props;
    const {
        id, brand, name, image, price
    } = product
}


export default Products